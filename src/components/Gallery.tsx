import styles from './Gallery.module.css';

export default function Gallery() {
  return (
    <section id="work" className={styles.gallery}>
      {/* Placeholder — full-width image sequence coming next */}
    </section>
  );
}
