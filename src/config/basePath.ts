/**
 * Base path for GitHub Pages deployment.
 * All static asset paths (images, fonts) must be prefixed with this
 * since we use plain <img> tags, not next/image.
 */
export const BASE_PATH = '/honeydew-associates-site-or-honeydew-realtors';
